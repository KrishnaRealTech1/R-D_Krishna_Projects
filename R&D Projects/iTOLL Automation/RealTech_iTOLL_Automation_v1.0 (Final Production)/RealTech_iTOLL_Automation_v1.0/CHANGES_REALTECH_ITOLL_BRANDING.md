# RealTech iTOLL Automation branding and UI update

- Added the supplied RealTech iTOLL Automation logo as an embedded WPF resource.
- Generated and applied a multi-resolution Windows application icon.
- Changed the main window and dialog titles to **RealTech iTOLL Automation**.
- Updated executable product metadata and publish output name to `RealTechiTOLLAutomation.exe`.
- Added a branded dashboard card containing the logo and application name.
- Increased the default/minimum window size and widened the status dashboard.
- Replaced fixed dashboard row heights with proportional rows so cards expand to fill the available height.
- Removed the fixed bottom alignment from Capture Storage, eliminating the empty vertical gap.
- Increased the log panel height and normalized outer spacing.
- Increased the Capture Storage card's minimum height and changed its internal rows to content-sized rows so the full storage description remains visible.
- Rebalanced the dashboard and log panel heights to preserve space for all status cards on smaller displays.
